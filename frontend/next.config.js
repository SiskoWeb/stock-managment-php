/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'https://fastly.picsum.photos', 'https://picsum.photos']
    }
}

module.exports = nextConfig
